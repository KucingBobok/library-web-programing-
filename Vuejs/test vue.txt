
const hostname = '127.0.0.5';

const express = require('express');

var cors = require('cors')

const app = express();
const port = 80;
app.use(express.json());

app.use(cors())

app.get('/bagianke01', (req, res) => {
  res.send('halaman berita politik')});

app.post("/bagianke01", (req, res) => {
  // Disini kodingan untuk menyimpan data kedalam database
  res.send(
    "Suhu : " +
      req.body.suhu +
      " intensitas cahaya " +
      req.body.intensitascahaya +
      " motion sensor " +
      req.body.motionsensor
  );
});

app.get('/bagianke02', (req, res) => {
  res.send('halaman berita otomotif')});

app.get('/bagianke04', (req, res) => {
  res.send({bagianke02: ahmad, hanif, vido})});

app.get('/bagianke05', (req, res) => {
  res.send({bagianke03: 01, 02, 03})});


app.listen(port, () => {
  console.log(`cli-nodejs-api listening at http://localhost:${port}/`)
});


//(http://127.0.0.3/)

